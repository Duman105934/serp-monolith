# 🪴 Правила именования веток (Git Branching Rules)

Этот документ описывает правила создания, именования и использования веток в проекте.  
Он помогает поддерживать чистую историю и единый стиль работы даже при небольших командах.

---

## 📘 Общая структура имени ветки

`<type>/<scope>/<ticket?>-<slug>`

### 🔹 Где:
- **type** — тип изменения (см. список ниже)
- **scope** — область или модуль (ui, api, db, gateway, worker, etc.)
- **ticket** *(опционально)* — ID задачи (например, SME-123)
- **slug** — короткое описание в kebab-case

**Примеры:**</br>
- feat/ui/SME-123-login-screen</br>
- fix/api/refresh-token-500</br>
- refactor/worker/queue-retry-policy</br>
- chore/deps/quasar-2-18-5</br>
- docs/readme/update-links</br>
- hotfix/api/null-pointer-on-auth</br>
- release/1.2.0</br>

---

## 📂 Допустимые типы веток (`<type>`)

| Тип | Описание |
|------|-----------|
| **feat** | Новая функциональность |
| **fix** | Исправление ошибки |
| **refactor** | Рефакторинг кода без изменения логики |
| **docs** | Изменения документации |
| **style** | Изменения оформления (CSS, форматирование и т.п.) |
| **test** | Добавление или изменение тестов |
| **perf** | Оптимизация производительности |
| **build** | Изменения сборки, Docker, Vite, CI/CD |
| **ci** | Настройка или правка pipeline |
| **chore** | Рутинные задачи (обновления пакетов, конфиги) |
| **hotfix** | Срочное исправление ошибки в проде |
| **release** | Подготовка нового релиза |

---

## 🧱 Правила именования

1. Используй **только** латиницу, цифры, дефисы (`-`) и слеши (`/`).
2. Всё в нижнем регистре (кроме ID тикета, например `SME-123`).
3. Описание (`slug`) — короткое, осмысленное, в формате **kebab-case**.
4. Для релизов — формат `release/<semver>` (например, `release/1.2.0`).
5. Для критических правок — `hotfix/<scope>/<slug>`.
6. Не начинай имя ветки с номера задачи.
7. После мёрджа ветку **удаляй**, если она больше не нужна.

---

## 🧾 Регулярные выражения для валидации

**С тикетом:**
^(feat|fix|refactor|docs|style|test|perf|build|ci|chore|hotfix|release)/[a-z0-9-]+/[A-Z]+-\d+-[a-z0-9-]+$

**Без тикета:**
^(feat|fix|refactor|docs|style|test|perf|build|ci|chore|hotfix|release)/[a-z0-9-]+/[a-z0-9-]+$

**Комбинированное правило (оба варианта допустимы):**
^(feat|fix|refactor|docs|style|test|perf|build|ci|chore|hotfix|release)/[a-z0-9-]+/([A-Z]+-\d+-)?[a-z0-9-]+$